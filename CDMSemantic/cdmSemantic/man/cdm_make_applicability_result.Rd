% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdm_applicability.R
\name{cdm_make_applicability_result}
\alias{cdm_make_applicability_result}
\title{Build a standardised applicability result list}
\usage{
cdm_make_applicability_result(shacl_result, data, methodology, condition)
}
\arguments{
\item{shacl_result}{List returned by \code{shapeR::validate_shacl()}, containing
\code{conforms} (logical) and \code{results} (data frame of violations).}

\item{data}{The \code{data} argument that was passed to the calling
\verb{check_applicability_*()} function — used to record the project IRI in
the attestation when \code{data} is a character IRI.}

\item{methodology}{Character scalar. Short methodology code, e.g.
\code{"ACM0002"} or \code{"AMS-I.A"}.}

\item{condition}{Character scalar. Name of the applicability condition
being checked, e.g. \code{"RenewableEnergyTechnology"}.}
}
\value{
A list with three elements:
\describe{
\item{\code{conforms}}{Logical — \code{TRUE} if the condition is met.}
\item{\code{violations}}{Data frame of SHACL violation details (empty when
\code{conforms = TRUE}).}
\item{\code{attestation}}{Named list recording \code{project_id}, \code{methodology},
\code{condition}, \code{checked_at}, and \code{prima_facie}.}
}
}
\description{
Wraps a SHACL validation result and attaches attestation metadata. This is
the single return-value constructor used by all \verb{check_applicability_*()}
functions across CDM methodology packages.
}
