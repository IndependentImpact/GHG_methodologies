% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdm_triples.R
\name{cdm_resolve_triples}
\alias{cdm_resolve_triples}
\title{Resolve a project description to a triples data frame}
\usage{
cdm_resolve_triples(data, fluree_conn)
}
\arguments{
\item{data}{Either a single character project IRI or a data frame with
columns \code{subject}, \code{predicate}, \code{object} (and optionally \code{datatype}).}

\item{fluree_conn}{A connected \code{FlureeInstance} (novaRush). Required when
\code{data} is a character IRI.}
}
\value{
Data frame with columns \code{subject}, \code{predicate}, \code{object}, \code{datatype}.
}
\description{
Dispatches on the type of \code{data}:
\itemize{
\item Character scalar: fetches all triples for the project IRI from Fluree via
a 2-hop SPARQL SELECT query (requires \code{fluree_conn}).
\item Data frame: validates that it has \code{subject}, \code{predicate}, \code{object} columns
and returns it unchanged (local / test path).
}
}
