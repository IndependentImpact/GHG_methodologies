% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ams_iiq_applicability.R
\name{check_applicability_efficiency_gain_iiq}
\alias{check_applicability_efficiency_gain_iiq}
\title{Check energy intensity improvement (Applicability Condition 3)}
\usage{
check_applicability_efficiency_gain_iiq(
  baseline_data,
  project_data,
  group_cols = NULL,
  minimum_improvement = 0.05,
  baseline_energy_col = "baseline_energy_use_mwh",
  baseline_service_col = "baseline_service_output_mwh",
  project_energy_col = "project_energy_use_mwh",
  project_service_col = "project_service_output_mwh"
)
}
\arguments{
\item{baseline_data}{Tibble containing baseline monitoring observations.}

\item{project_data}{Tibble containing project monitoring observations.}

\item{group_cols}{Optional character vector of grouping columns shared across
both datasets.}

\item{minimum_improvement}{Minimum fractional improvement (e.g. \code{0.05} for a
5\% efficiency gain).}

\item{baseline_energy_col}{Column storing baseline energy use.}

\item{baseline_service_col}{Column storing baseline service output.}

\item{project_energy_col}{Column storing project energy use.}

\item{project_service_col}{Column storing project service output.}
}
\value{
\code{TRUE} when every group achieves at least the minimum improvement; the
function returns \code{FALSE} when any group falls short.
}
\description{
AMS-II.Q projects must demonstrate an efficiency gain relative to the
baseline. The helper compares baseline and project energy intensities and
confirms that the relative improvement meets the methodology's minimum
threshold.
}
\examples{
baseline <- tibble::tibble(
  building_id = c("A", "B"),
  baseline_energy_use_mwh = c(800, 600),
  baseline_service_output_mwh = c(720, 540)
)
project <- tibble::tibble(
  building_id = c("A", "B"),
  project_energy_use_mwh = c(520, 420),
  project_service_output_mwh = c(720, 540)
)
check_applicability_efficiency_gain_iiq(
  baseline,
  project,
  group_cols = "building_id",
  minimum_improvement = 0.05
)
}
