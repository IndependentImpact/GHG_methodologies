% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ams_ie_equations.R
\name{calculate_non_renewable_biomass}
\alias{calculate_non_renewable_biomass}
\title{Calculate non-renewable biomass consumption (Equation 1)}
\usage{
calculate_non_renewable_biomass(
  biomass_data,
  consumption_col = "biomass_consumption_tonnes",
  fraction = 1,
  fraction_col = "non_renewable_fraction",
  group_cols = NULL,
  output_col = "non_renewable_biomass_tonnes"
)
}
\arguments{
\item{biomass_data}{A tibble containing biomass consumption observations.}

\item{consumption_col}{Column name storing biomass consumption (e.g. tonnes).}

\item{fraction}{Fraction of the biomass that is non-renewable. Supply either a
single numeric value or a vector matching the number of rows in
\code{biomass_data} when \code{fraction_col} is \code{NULL}.}

\item{fraction_col}{Optional column containing the non-renewable biomass
fraction for each observation. When provided it overrides \code{fraction}.}

\item{group_cols}{Optional character vector of grouping columns used to
aggregate the non-renewable biomass consumption.}

\item{output_col}{Name of the resulting column containing non-renewable
biomass in the same units as \code{consumption_col}.}
}
\value{
A tibble with grouping columns (if supplied) and a column named
\code{output_col} storing non-renewable biomass consumption.
}
\description{
Implements Equation (1) of AMS-I.E by applying the fraction of non-renewable
biomass to the observed biomass consumption and aggregating across the
specified grouping variables.
}
\examples{
data <- tibble::tibble(
  user_id = c("cookstove-1", "cookstove-2"),
  biomass_tonnes = c(15, 18),
  non_renewable_fraction = c(0.85, 0.9)
)
calculate_non_renewable_biomass(
  data,
  consumption_col = "biomass_tonnes",
  fraction_col = "non_renewable_fraction"
)
}
